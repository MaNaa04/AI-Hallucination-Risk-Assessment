version: '3.8'

services:
  backend:
    build: .
    container_name: ai-hallucination-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
